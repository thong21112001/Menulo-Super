<div class="modal fade" id="saleResListModal" tabindex="-1"
     aria-labelledby="saleResListTitle" data-bs-backdrop="static" data-bs-keyboard="false" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered modal-lg modal-dialog-scrollable">
        <div class="modal-content">

            <div class="modal-header">
                <h5 class="modal-title" id="saleResListTitle">
                    <i class="bi bi-card-list me-1"></i> Danh sách nhà hàng
                </h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Đóng"></button>
            </div>

            <div class="modal-body">

                <div id="sale-res-loading" class="text-center py-5 d-none">
                    <div class="spinner-border" role="status" aria-hidden="true"></div>
                    <div class="mt-2">Đang tải...</div>
                </div>

                <div id="sale-res-empty" class="alert alert-warning d-none mb-0">
                    <i class="bi bi-exclamation-triangle me-1"></i>
                    Sale này chưa tạo nhà hàng nào.
                </div>

                <div id="sale-res-list-container" class="d-none">
                    <table class="table table-sm table-striped table-hover">
                        <thead class="table-light">
                            <tr>
                                <th scope="col" class="text-center">#</th>
                                <th scope="col">Tên nhà hàng</th>
                                <th scope="col">Địa chỉ</th>
                                <th scope="col">Số điện thoại</th>
                            </tr>
                        </thead>
                        <tbody id="sale-res-list-tbody">
                        </tbody>
                    </table>
                </div>

            </div>

            <div class="modal-footer">
                <button class="btn btn-secondary" data-bs-dismiss="modal">Đóng</button>
            </div>
        </div>
    </div>
</div>