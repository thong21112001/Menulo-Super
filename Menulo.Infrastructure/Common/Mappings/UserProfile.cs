using AutoMapper;

namespace Menulo.Infrastructure.Common.Mappings
{
    public sealed class UserProfile : Profile
    {
        //public UserProfile()
        //{
        //    // Định nghĩa cách map TỪ ApplicationUser (Infrastructure)
        //    // TỚI SaleRowDto (Application)
        //    CreateMap<ApplicationUser, SaleRowDto>()
        //        .ForMember(dest => dest.UserId, opt => opt.MapFrom(src => src.Id))
        //        .ForMember(dest => dest.Username, opt => opt.MapFrom(src => src.UserName));
        //}
    }
}
